version: "3.8"

services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: zhixin
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - zhixin_pg:/var/lib/postgresql/data

volumes:
  zhixin_pg:
